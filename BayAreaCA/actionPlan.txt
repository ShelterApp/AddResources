Code needs to:
1. Get weekly updates which are done by https://biglocalnews.org Stanford volunteers
  Done Automatically via an Azure function with it's own code
  Updates require an API token with 'biglocalnews' account
  account will be: S.A.
  Secret will be kept seperately.

 
  2. Weekly data gets downloaded into temp DB

  4. clean up and organize tempDB download into the 14 key names needed for tmpCollections so it matches with 
      services collections

  5. let others know the data is ready to be confirmed, then pushed to mainDB.

